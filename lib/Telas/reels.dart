import 'package:flutter/material.dart';

class Reels extends StatelessWidget {
  const Reels({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Em Andamento...', style: TextStyle(color: Colors.white, fontSize: 40),),
    );
  }
}
